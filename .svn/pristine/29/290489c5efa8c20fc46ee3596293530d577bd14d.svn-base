package locator.service;

import locator.preview.Location;

public interface IssInformationService { //Feedback: pollution of interface. The user of this interface
  //does not care about *all* these functions. They only care about one, the first one. We should not confuse interface with implementation (what we do vs. how we do it).
  String getFlyOverTimeForLocation(Location location);

  String getRawResponse(String url); //Feedback: please remove

  String parseJSON(String rawJSON); //Feedback: please remove

  String getFlyOverTime(Location location); //Feedback: please remove
}
