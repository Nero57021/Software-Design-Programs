- given a string of character with only a-z and lower cased sequence, upperCaseConverterBlock return uppercase string
- given a string of character with a-z and upper cased sequence, upperCaseConverterBlock return uppercase string
- given a string of character with a-z and mix case sequence, upperCaseConverterBlock return uppercase string
- given a string of character with only numbers sequence, upperCaseConverterBlock return uppercase string
- given a string of character with numbers and a-z lower case sequence, upperCaseConverterBlock return uppercase string
- given a string of character with numbers and a-z uppercase sequence, upperCaseConverterBlock return uppercase string
- given a string of character with numbers and a-z mix case sequence, upperCaseConverterBlock return uppercase string
- given a empty string, upperCaseConverterBlock return empty string

- given a string of character with only a-z and lower cased sequence, lowerCaseConverterBlock return uppercase string
- given a string of character with a-z and upper cased sequence, lowerCaseConverterBlock return uppercase string
- given a string of character with a-z and mix case sequence, lowerCaseConverterBlock return uppercase string
- given a string of character with only numbers sequence, lowerCaseConverterBlock return uppercase string
- given a string of character with numbers and a-z lower case sequence, lowerCaseConverterBlock return uppercase string
- given a string of character with numbers and a-z uppercase sequence, lowerCaseConverterBlock return uppercase string
- given a string of character with numbers and a-z mix case sequence, lowerCaseConverterBlock return uppercase string
- given a empty string, lowerCaseConverterBlock return empty string